🔐 Мониторинг SSL сертификатов в Zabbix и вывод метрик в Grafana

📌 Варианты реализации

1. Web-сценарии Zabbix – самый простой и встроенный способ.
      ✔️ Не требует скриптов.
      ✔️ Автоматически создаются элементы данных:
   · web.test.ssl.certificate.expiry[<имя_сценария>] – дней до окончания.
   · web.test.ssl.certificate.valid[<имя_сценария>] – статус валидности (1 – ок, 0 – проблема).
2. Внешние проверки (External check / UserParameter) – если нужно проверять сертификаты на серверах, к которым у Zabbix сервера нет прямого HTTP-доступа, или нужна более гибкая логика.
      ✔️ Используется openssl, curl или certbot.

Рассмотрим оба подхода.

---

✅ 1. Мониторинг через встроенные Web-сценарии Zabbix

Шаг 1. Создание Web-сценария

· Перейдите в Configuration → Hosts → (ваш хост) → Web.
· Нажмите Create web scenario.
· Name: например, SSL_google.com.
· Application: укажите существующую или создайте новую (например, SSL).
· Во вкладке Steps добавьте шаг:
  · Name: Home page
  · URL: https://example.com
  · Required status codes: 200 (можно оставить пустым).
· Во вкладке Authentication ничего не меняем.
· SSL certificate validation – обязательно поставьте галочку.
· Verify host – рекомендуется оставить включённым (проверка соответствия имени хоста в сертификате).

После сохранения Zabbix автоматически создаст два элемента данных:

Ключ Тип Описание
web.test.ssl.certificate.expiry[SSL_google.com] Numeric (float) Осталось дней до истечения сертификата
web.test.ssl.certificate.valid[SSL_google.com] Numeric (unsigned) 1 – сертификат действителен, 0 – недействителен или ошибка

Шаг 2. Триггер (опционально)
Создайте триггер, который сработает, например, когда осталось менее 30 дней:

```
{host:web.test.ssl.certificate.expiry[SSL_google.com].last()} < 30
```

---

✅ 2. Альтернативный способ – через скрипт и UserParameter (если web-сценарий не подходит)

Пример получения даты окончания сертификата через OpenSSL:

```bash
#!/bin/bash
# /usr/lib/zabbix/externalscripts/ssl_expiry.sh
SITE="$1"
PORT="${2:-443}"
echo | openssl s_client -servername "$SITE" -connect "$SITE:$PORT" 2>/dev/null | \
openssl x509 -noout -enddate | cut -d= -f2
```

UserParameter в конфигурации агента (zabbix_agentd.conf):

```
UserParameter=ssl.cert.expiry[*],/usr/lib/zabbix/externalscripts/ssl_expiry.sh "$1" "$2"
```

Элемент данных на хосте:

· Тип: Zabbix agent
· Ключ: ssl.cert.expiry[example.com,443]
· Тип информации: Текст – получим строку типа Feb 11 23:59:59 2026 GMT.

Чтобы преобразовать дату в дни до окончания, понадобится зависимый элемент данных с предобработкой. Это сложнее, поэтому рекомендую использовать первый способ (Web-сценарии) везде, где это возможно.

---

📊 Отображение в Grafana

1. Установите плагин Zabbix datasource
Выполните на сервере Grafana:

```bash
grafana-cli plugins install alexanderzobnin-zabbix-datasource
```

Перезапустите Grafana.

2. Добавьте источник данных

· Configuration → Data Sources → Add data source.
· Выберите Zabbix.
· Укажите URL Zabbix API (обычно http://zabbix-server/api_jsonrpc.php).
· Логин/пароль (пользователь с правами на чтение).
· Настройте остальные параметры (например, тренды).
· Save & test.

3. Создайте панель с метрикой SSL expiry

Вариант A – как числовое значение (Gauge / Stat)

· В редакторе запроса выберите Group, Host, Application (например, SSL).
· Item: начните вводить web.test.ssl.certificate.expiry. Выберите нужный сценарий.
· В поле Options можно задать функцию агрегации (последнее значение).
· Тип панели – Stat или Gauge.

Вариант Б – временной ряд (Time series)

· Тот же элемент данных, но на панели типа Time series.
· Будут отображаться исторические данные об уменьшении количества дней.

Пример запроса (в плагине Zabbix):

```
item: web.test.ssl.certificate.expiry["SSL_google.com"]
```

Вариант В – проверка валидности (0/1)
Используйте элемент web.test.ssl.certificate.valid[...].
На панели можно сделать условную окраску: зелёный при 1, красный при 0.

---

🧩 Полезные советы

· Массовое создание Web-сценариев – используйте шаблоны. Например, создайте шаблон с готовым сценарием, параметризовав URL через макрос {HOST.CONN} или пользовательский макрос {$SSL_URL}.
· Для HTTPS-портов, отличных от 443, укажите полный URL с портом: https://example.com:8443.
· Если сайт использует редирект, в шаге сценария укажите конечный URL или разрешите следовать редиректам (галочка Follow redirects).
· Метрика web.test.ssl.certificate.expiry обновляется после каждого выполнения сценария (по умолчанию интервал 1 мин). В Grafana можно настроить кэширование данных.

---

📌 Итог

1. Zabbix – Web-сценарий с включённой валидацией SSL.
2. Grafana – Zabbix datasource, запрос к элементу web.test.ssl.certificate.expiry[<имя>].

Этого достаточно для полноценного мониторинга и визуализации сроков действия SSL-сертификатов.